<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="X-UA-Compatible" content="ie=edge">
<title>Iter</title>
<script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<link rel="shortcut icon" href="{{storageUrl}}/static/favicon.ico">
<link rel="stylesheet" href="/css/styles.css">
</head>
<body>
	<main id="login">
		<div class="login__column">
			<img src="{{storageUrl}}/static/phoneImage.png" />
		</div>
		<div class="login__column">
			<div class="login__box">
				<img src="{{storageUrl}}/static/loginLogo.png" />
				<form action="/auth/join" method="post" class="login__form" onsubmit="return check_information(this)">
					<input type="email" name="email" placeholder="Email" required> 
					<input type="text" name="name" placeholder="Name" required> 
					<input type="text" name="username" placeholder="Username" required> 
					<input id="pw" type="password" name="password" placeholder="Password" required>
                    <input id="pwre" type="password" name="passwordRe" placeholder="Re-Type Password" required>
					<input type="submit" value="Sign up">
				</form>
<!--				<span class="login__divider"> or </span> -->
<!--				-->
<!--				<a href="#" class="login__small-link">Forgot password?</a>-->
			</div>
			<div class="login__box">
				<span class="login__text"> Did you have an account? <br />
				</span> <a class="login__blue-link" href="/auth/loginForm">Sign in</a>
			</div>
		</div>
	</main>

    <script>
        function check_information() {
            var pw = document.getElementById( 'pw' ).value;
            var pw_re = document.getElementById( 'pwre' ).value;

            if(pw !== pw_re) {
                alert("비밀번호가 일치하지 않습니다.");
                return false;
            }

            return true;
        }

    </script>
	{{>layout/footer}}