{{>layout/header}}
<main id="profile">
		<div class="profile__photo-grid">
				{{#posts}}
				<!-- 유저가 업로드한 Feed 사진 -->
					<div class="profile__photo">
						<a href="/user/{{user.id}}">
                            {{#images}}
                                <img src="https://{AZURE_ACCOUNT_NAME}.blob.core.windows.net/photo/{{url}}" />
                            {{/images}}
						</a>	
					</div>									
					{{/posts}}
		</div>	
</main>

{{>layout/footer}}
